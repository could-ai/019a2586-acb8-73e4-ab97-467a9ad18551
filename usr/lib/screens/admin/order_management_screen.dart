import 'package:flutter/material.dart';

class AdminOrderManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Order Management & Overrides')),
      body: Center(child: const Text('Manual order overrides and tracking.')),
    );
  }
}
