import 'package:flutter/material.dart';

class OffersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Offers & Visibility')),
      body: Center(child: const Text('Create and manage promotions or featured listings.')),
    );
  }
}
